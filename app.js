'use strict';
(() => {
const zh = {
navOverview:'研究概览',navMethod:'方法',navResults:'实验结果',navAudio:'效果试听',navRadius:'连续半径',heroLabel:'论文初稿 · 交互展示',heroSubtitle:'一个麦克风，一个可调的聆听区域。提取指定半径内的声音，并在目标集合不变时约束输出保持稳定。',listenAction:'聆听效果样例',paperAction:'阅读论文初稿',draftNote:'研究初稿 · 作者信息将在正式发布前补充。',regionLabel:'半径决定聆听范围',insideRegion:'区域内：目标语音',outsideRegion:'区域外：抑制',schematicNote:'任务概念示意，非实测场景',specInput:'输入形式',specMono:'单通道语音',specQuery:'查询范围',specStep:'浏览步长',specModel:'展示模型',specCheckpoint:'连续半径 + 一致性 · ep48',overviewLabel:'研究思路',overviewTitle:'选择一个区域。<br>听见其中的声音。',overviewLead:'让距离成为语音提取的控制条件：目标为距离小于查询半径的全部说话人语音之和。',equationNote:'严格边界：仅当 dᵢ < r 时，该说话人才进入目标集合。',abstractTitle:'摘要 · 内容概述',abstractText:'本研究探索查询半径连续可调的单通道区域语音提取。连续半径条件使同一个模型能够选择不同空间范围内的目标；跨半径一致性学习则在两次查询包含相同说话人时，约束预测输出保持稳定。实验从完整目标保留、部分目标提取、空目标拒绝，以及说话人数和噪声变化等方面进行考察。',abstractLink:'阅读完整摘要与问题定义 ↗',feat1Title:'连续控制',feat1Body:'通过半径查询调整区域，不局限于少数几个固定提取档位。',feat2Title:'跨半径一致性',feat2Body:'当相邻查询包含的目标集合相同时，鼓励模型输出保持一致。',feat3Title:'一个模型，三种目标状态',feat3Body:'保留区域内全部语音、提取其中一部分，或在区域无人时抑制输出。',methodLabel:'方法设计',methodTitle:'学习何时改变。<br>约束何时保持。',methodLead:'对同一个混音使用不同半径查询。仅在目标集合相同且非空时，施加训练一致性约束。',figureCaption:'所附论文中的模型架构与半径条件化细节。完整网络及训练目标见原文。',step1Title:'混音与查询半径',step1Body:'单通道混音与连续标量共同定义一次区域提取任务。',step2Title:'条件化提取',step2Body:'三层 MLP 编码半径，通过 FiLM 调制时频提取网络。',step3Title:'跨查询一致',step3Body:'使用相同且非空的目标集合，提供跨查询波形一致性监督。',resultsLabel:'实验结果',resultsTitle:'不只衡量分离能力。',resultsLead:'语音保留、目标选择与区域拒绝对应不同任务，各用相应指标衡量。',metricFull:'完整目标保留',metricPartial:'部分目标提取',metricEmpty:'空目标拒绝',metricSource:'已复测的 0–2 源干净评测集 · ep48 · 21 profile × 3 混音 · 半径 1/2/3/4 m · 每格中位数后求均值。',ablationTitle:'消融实验',manuscriptTag:'初稿表 1 报告值',radiusMode:'半径条件',consistency:'一致性',ablationNote:'此表按初稿原值展示，RCE 尚未复现到报告数值。统一离散基线采样权重的重训尚只有阶段性记录，因此此表不能作为已完成的同分布消融结论。',challengingTitle:'更多说话人与带噪场景',verifiedTag:'已复测结果',condition:'评测条件',outputs:'输出数',challengeNote:'“2-spk”和“3-spk”分别指覆盖 0–2 源、0–3 源的 profile 评测集，并非只统计恰好两人或三人。均使用同一 ep48 模型，带噪输入对应干净语音目标。SI-SDRi 是相对输入的改进量，变高不等于输出绝对质量更好。',provenanceSummary:'关于初稿结果与复现口径',provenanceBody:'初稿带噪结果与本次复测存在明显差异，上表展示可核验的复测数值。四档半径的干净评测在 135 对目标不变的相邻查询上，平均 RCE 为 4.43%，未复现初稿的 0.72%。比较 RCE 前需统一半径间隔及聚合方法。下方密半径扫描使用不同 seed，不能与本表作逐样本对照。',audioLabel:'音频效果展示',audioTitle:'同一段混音。<br>不同的聆听范围。',audioLead:'对照输入与模型输出。切换查询半径，试听保留的语音如何变化。',audioNote:'精选定性样例，并非随机总体样本。输入与输出保留原始保存幅度，空目标输出可能接近静音；未对各音频单独做响度归一化。',radiusLabel:'连续半径浏览',radiusTitle:'看见固定档位之间的变化。',radiusLead:'浏览 0–4 m 范围内每隔 0.1 m 的全部查询。虚线标出真实声源距离，曲线显示实测输出电平。',selectSample:'选择混音样本',presetSingle:'单说话人',presetTwo:'双说话人',presetTransition:'较宽过渡区',axisLabel:'纵轴指标',axisAbsolute:'输出 RMS 电平（dB）',axisRelative:'相对输入电平（dB）',curveLegend:'实测输出',inspectRadius:'查看半径',curveNote:'0.1 m 是查询采样间隔，不代表 0.1 m 的定位精度。过渡可能早于或晚于真实声源距离；纳入第二个说话人并不保证总 RMS 显著增大。',curveCount:'混音数 × 查询点数',curveMedian:'目标非空且不变时，每步电平绝对变化中位数',curveStable:'此类 1,168 个相邻步中，变化不超过 1 dB 的比例',downloadShort:'↓ 缩略 CSV · 3 样本 / 123 行',downloadFull:'↓ 完整 CSV · 63 样本 / 2,583 行',downloadSample:'下载当前曲线 ↓',resourcesLabel:'研究资料',resourcesTitle:'阅读、聆听、复查。',resourcesLead:'论文初稿、所选音频和完整半径查询数据均随展示页一同提供。',resourcePaper:'阅读论文初稿',resourcePaperNote:'原始附件 · 共 5 页',resourceData:'完整查询数据',resourceDataNote:'63 段混音 · 0–4 m · 0.1 m 步长',resourceProvenance:'实验来源记录',resourceProvenanceNote:'检查点、seed 和样本标识',citationNote:'作者、单位、会议与正式引用待确定后补充。本页不表示论文已发表或已录用。',footerNote:'研究展示 · 纯静态页面',backTop:'返回顶部 ↑',
input:'原始输入',output:'模型输出',target:'目标人数',level:'输出电平',delta:'较上一档',sources:'声源距离',none:'无语音声源',outside:'超出图示范围',sample:'样本',profile:'Profile',radius:'查询半径',noExamples:'音频数据未加载，请检查 assets/data/audio.js 文件。',fullCategory:'完整目标保留',partialCategory:'部分目标提取',emptyCategory:'空目标抑制',threeCategory:'三说话人场景',noisyCategory:'10 dB 带噪场景',waveNote:'波形统一使用本组输入峰值作基准',audioFailure:'音频未能加载，请检查部署文件。'
};
Object.assign(zh, {
  "heroSubtitle": "一个麦克风，一个可调的聆听区域。以半径查询目标语音；当目标说话人集合不变时，鼓励模型输出保持一致。",
  "specCheckpoint": "连续半径 + 一致性 · EXP6 / ep48",
  "implementationTag": "与实际实现对齐的网络结构",
  "offlineTag": "离线 · 非因果",
  "frontDetail": "192 点 FFT · 帧移 128 samples<br>递归幅度归一化<br>实部 + 虚部 + 对数幅度",
  "backboneDetail": "频率双向 LSTM + 残差<br>时间双向 LSTM + 残差<br>未启用注意力分支",
  "decodeDetail": "32 → 2 个复数谱平面<br>乘回归一化尺度<br>ISTFT → 一条目标叠加波形",
  "radiusPath": "半径条件分支",
  "injectionPath": "五个位于 BLOCK 之间的调制层",
  "filmDetail": "两条 1×1 Conv1d 生成 γ 和 β：H′ = γ(eᵣ) ⊙ H + β(eᵣ)。参数随频率变化，沿时间广播。",
  "singleOutput": "单输出 ≠ 单目标说话人",
  "figureCaption": "本图依据最终 EXP6 配置与前向代码整理，不再沿用初稿中的结构图。输出为区域内所有目标说话人的波形之和，并非独立的多条说话人轨道。",
  "trainingSummary": "训练目标与实现细节",
  "samplingTitle": "一个混音，四个查询",
  "samplingBody": "训练分别从 [0,1)、[1,2)、[2,3)、[3,4) m 各抽一个半径；验证固定为 1/2/3/4 m。EXP6 从离散版 best48 主干初始化并重建半径编码器，同时使用重加权的训练 profile。",
  "lossTitle": "一致性约束，不是精确边界检测器",
  "lossBody": "仅保留相邻、目标集合相同且非空、公共目标 RMS > 10⁻⁴ 的查询对；无有效对时一致性项为零。主损失使用 SNRLP、40 上限与直通梯度；零目标使用 100 × MAE(输出)。",
  "resultsLabel": "验证集诊断",
  "resultsTitle": "保留、选择与抑制。",
  "resultsLead": "基于已保存的验证结果，不是新运行的独立测试集评估。Full / Partial / Empty 按目标说话人集合划分，不按 SI-SDRi 是否存在划分。",
  "metricSource": "EXP6 / ep48 · 0–2 源验证集 · 21 profile × 3 混音 · 半径 1/2/3/4 m。指标卡与带噪表：每个 profile×半径格子的 3 条混音取中位数，再平均。Clean 仅指含语音混音不额外加噪；0 源样本仍为纯噪声。",
  "ablationTitle": "模型对比 · 统一统计口径",
  "manuscriptTag": "非严格受控消融",
  "comparisonCaption": "采用统一 profile 均值口径比较已有结果；来源未确认的基线不展示数值。",
  "samplingColumn": "训练采样",
  "unresolved": "来源待确认",
  "uniformProfiles": "均匀 · 21 类",
  "weightedProfiles": "重加权 · W=56",
  "ablationNote": "三个有来源的模型均使用 profmean：先对每个 profile 内属于该目标类别的混音/半径记录求均值，再对 profile 等权平均；不同于上方的每格中位数口径。同时训练采样也发生了改变，因此不能把差异只归因于一致性项。初稿离散无一致性基线的来源尚未确认，暂不展示。",
  "rceSummary": "为什么暂不展示 RCE 主指标或“最佳模型”标签？",
  "rceBody": "初稿混用了聚合方式。其中 0.81% 与 0.85% 对应同目标查询对的中位数，而不是公式所写的均值；最终模型 0.72% 的来源仍未确认。需先统一检查点、半径网格、有效对筛选和聚合方式，再展示 RCE。现有结果不支持模型在所有指标上全面最优的结论。",
  "verifiedTag": "已有结果重算",
  "challengeNote": "0–2 源套件含 63 条混音；0–3 源套件含 168 条。均为 EXP6 / ep48，采用每格中位数后平均。Full 为全部语音源入选，Partial 为部分入选，Empty 为没有入选源。目标保留混响语音，但不含外加噪声；纯噪声样本不定义语音 SNR。SI-SDRi 较大不一定代表输出绝对质量更好。",
  "provenanceBody": "这些数值由已保存的验证结果重算；本次网页修订没有重新推理。初稿带噪结果与之明显不同，原始生成管线仍待确认，不能仅凭差异断言原因。下方密集半径扫描是另一组混音，不是与此表配对的对比。",
  "audioLead": "比较输入与 1/2/3/4 m 四档预计算输出。这些是已保存的试听样例，不是在浏览器中实时运行模型。",
  "resourcePaperNote": "原始初稿 · 图表与结果仍有已知差异",
  "resourceProvenanceNote": "随站点提供的来源记录 · 核查范围见 README"
});
const en = {input:'Original input',output:'Model output',target:'Target speakers',level:'Output level',delta:'Step change',sources:'Source distances',none:'No speech source',outside:'outside plot range',sample:'Sample',profile:'Profile',radius:'Query radius',noExamples:'Audio data unavailable. Check assets/data/audio.js.',fullCategory:'Full-target preservation',partialCategory:'Partial-target extraction',emptyCategory:'Empty-target rejection',threeCategory:'Three-speaker mixture',noisyCategory:'10 dB noisy mixture',waveNote:'Waveform scale shared with this input',audioFailure:'Audio unavailable. Check deployed files.'};
const $=id=>document.getElementById(id); const initial={};
document.querySelectorAll('[data-i18n]').forEach(el=>{initial[el.dataset.i18n]=el.innerHTML});
let lang='en';try{lang=localStorage.getItem('radius-demo-lang')==='zh'?'zh':'en'}catch{}
const t=k=>(lang==='zh'?zh[k]:(initial[k]||en[k]))||initial[k]||en[k]||k;
const svgNS='http://www.w3.org/2000/svg';
function node(name,attrs={},text){const el=document.createElementNS(svgNS,name);for(const [k,v] of Object.entries(attrs))el.setAttribute(k,String(v));if(text!==undefined)el.textContent=text;return el}
function format(v,n=2){return Number.isFinite(v)?v.toFixed(n):'—'}
function signed(v){return Number.isFinite(v)?(v>0?'+':'')+v.toFixed(3):'—'}
const curves=window.CURVE_DATA?.samples||[]; let activeSample=1, pointIndex=10;
function setupSamples(){const select=$('sampleSelect');select.replaceChildren();for(const s of curves){const opt=document.createElement('option');opt.value=s.id;opt.textContent=`${t('sample')} ${String(s.id).padStart(2,'0')} · ${s.profile} · ${s.distances.length? s.distances.map(d=>d.toFixed(2)).join(' / ')+' m':t('none')}`;select.append(opt)}select.value=String(activeSample)}
function drawChart(){const s=curves.find(s=>s.id===activeSample);if(!s)return;const metric=Number($('axisSelect').value);const svg=$('radiusChart');svg.replaceChildren(node('title',{id:'chartTitle'},lang==='zh'?'查询半径与输出电平':'Output level by query radius'),node('desc',{id:'chartDesc'},lang==='zh'?'每隔0.1m的实测曲线，虚线为真实声源位置。':'Measured samples at 0.1 m intervals; dashed lines mark true source distances.'));
const W=1000,H=370,L=72,R=26,T=40,B=53,cw=W-L-R,ch=H-T-B;
const vs=s.points.map(p=>p[metric]);let lo=Math.floor(Math.min(...vs)/10)*10-10,hi=Math.ceil(Math.max(...vs)/10)*10+10;if(hi-lo<40){lo-=10;hi+=10}
const x=r=>L+r/4*cw,y=v=>T+(hi-v)/(hi-lo)*ch;
svg.append(node('text',{x:L,y:17,fill:'#68766e','font-size':12,'font-family':'system-ui'},metric===1?t('axisAbsolute'):t('axisRelative')));
for(let k=0;k<=5;k++){const v=lo+(hi-lo)*k/5;svg.append(node('line',{x1:L,x2:W-R,y1:y(v),y2:y(v),stroke:'#e7ece3'}));svg.append(node('text',{x:L-12,y:y(v)+4,'text-anchor':'end',fill:'#829080','font-size':12,'font-family':'system-ui'},v.toFixed(0)))}
for(let k=0;k<=8;k++){const v=k*.5;svg.append(node('text',{x:x(v),y:H-B+23,'text-anchor':'middle',fill:'#829080','font-size':12,'font-family':'system-ui'},v.toFixed(1)))}svg.append(node('text',{x:W/2,y:H-6,'text-anchor':'middle',fill:'#68766e','font-size':12,'font-family':'system-ui'},t('radius')+' (m)'));
for(let i=0;i<s.distances.length;i++){const d=s.distances[i];if(d>4)continue;svg.append(node('line',{x1:x(d),x2:x(d),y1:T,y2:H-B,stroke:'#b99455','stroke-dasharray':'5 5','stroke-width':1.3}));svg.append(node('text',{x:x(d)+5,y:T+13+i*17,fill:'#927341','font-size':11,'font-family':'system-ui'},`S${i+1} · ${d.toFixed(2)} m`))}
const pts=s.points.map(p=>`${x(p[0])},${y(p[metric])}`).join(' ');
svg.append(node('polygon',{points:`${L},${H-B} ${pts} ${W-R},${H-B}`,fill:'#e2efe7','fill-opacity':'.7'}));svg.append(node('polyline',{points:pts,fill:'none',stroke:'#287a60','stroke-width':2.6,'stroke-linejoin':'round','stroke-linecap':'round'}));
for(const p of s.points)svg.append(node('circle',{cx:x(p[0]),cy:y(p[metric]),r:2,fill:'#287a60'}));
const p=s.points[pointIndex];svg.append(node('line',{x1:x(p[0]),x2:x(p[0]),y1:T,y2:H-B,stroke:'#5b8170','stroke-dasharray':'2 4'}));svg.append(node('circle',{cx:x(p[0]),cy:y(p[metric]),r:5,fill:'#287a60',stroke:'#fff','stroke-width':2}));
$('curveInfo').textContent=`${t('sample')} ${s.id} · Profile ${s.profile} · ${t('sources')}: ${s.distances.length?s.distances.map(d=>d.toFixed(3)+' m'+(d>4?' ('+t('outside')+')':'')).join(' / '):t('none')}`;
$('radiusValue').textContent=p[0].toFixed(1)+' m';$('pointSummary').replaceChildren();for(const [label,value] of [[t('target'),String(p[2])],[t('level'),format(p[metric])+' dB'],[t('delta'),signed(pointIndex ? p[metric]-s.points[pointIndex-1][metric] : null)+' dB']]){const span=document.createElement('span');span.append(label+': ');const strong=document.createElement('strong');strong.textContent=value;span.append(strong);$('pointSummary').append(span)}
document.querySelectorAll('[data-sample]').forEach(b=>{const sel=Number(b.dataset.sample)===s.id;b.classList.toggle('active',sel);b.setAttribute('aria-pressed',String(sel))});
}
const groups=window.AUDIO_DATA?.groups||[];
const audioStates=new Map();
function wave(el,peaks,scale=1){el.replaceChildren();if(!peaks?.length)return;peaks.forEach((v,i)=>{const height=(v/scale)*36;el.append(node('rect',{x:i*5,y:21-Math.max(.5,height)/2,width:2.5,height:Math.max(.5,height),rx:1,fill:'#86b99a'}))})}
function makePlayer(label){const box=document.createElement('div');box.className='audio-player';const heading=document.createElement('div');heading.className='player-label';const title=document.createElement('span');title.textContent=label;heading.append(title);const svg=node('svg',{viewBox:'0 0 320 42',preserveAspectRatio:'none',class:'waveform','aria-hidden':'true'});const audio=document.createElement('audio');audio.controls=true;audio.preload='metadata';const detail=document.createElement('div');detail.className='audio-detail';box.append(heading,svg,audio,detail);audio.addEventListener('play',()=>document.querySelectorAll('audio').forEach(a=>{if(a!==audio)a.pause()}));audio.addEventListener('error',()=>{detail.textContent=t('audioFailure')});return {box,heading,title,svg,audio,detail}}
function audioMetric(f){const m=f.metrics||{};if(f.target_count===0)return ['Atten.',m.attenuation_db];if(Number.isFinite(m.si_sdri_db)&&f.target_count<(f.source_count||99))return ['SI-SDRi',m.si_sdri_db];return ['SI-SDR',m.si_sdr_db]}
function updateOutput(state,radius,changeSource=true){const {g,out,buttons}=state;const f=g.files.find(f=>f.role!=='input'&&f.radius===radius);if(!f)return;state.radius=radius;for(const b of buttons){const selected=Number(b.dataset.r)===radius;b.classList.toggle('active',selected);b.setAttribute('aria-pressed',String(selected))}
if(changeSource){out.audio.pause();out.audio.src=f.path;out.audio.load()}out.audio.setAttribute('aria-label',`${g.id} ${t('output')} ${radius} m`);wave(out.svg,f.waveform_peaks_64,g.waveScale);const m=f.metrics||{};const total=g.distances.length;const metric=f.target_count===0?['Atten.',m.attenuation_db]:f.target_count===total?['SI-SDR',m.si_sdr_db]:['SI-SDRi',m.si_sdri_db];out.detail.textContent=`${t('target')}: ${f.target_count}/${total} · ${metric[0]} ${format(metric[1])} dB`;
}
function buildAudio(){const wrap=$('audioExamples');if(!groups.length){wrap.textContent=t('noExamples');return}groups.forEach((g,index)=>{const card=document.createElement('article');card.className='audio-card';card.dataset.id=g.id;const head=document.createElement('div');head.className='audio-card-head';const num=document.createElement('span');num.className='audio-index';num.textContent=String(index+1).padStart(2,'0');const info=document.createElement('div');const title=document.createElement('h3');const meta=document.createElement('div');meta.className='audio-meta';info.append(title,meta);const cat=document.createElement('span');cat.className='audio-category';head.append(num,info,cat);const players=document.createElement('div');players.className='audio-players';const inp=makePlayer(t('input')),out=makePlayer(t('output'));players.append(inp.box,out.box);const input=g.files.find(f=>f.role==='input');inp.audio.src=input.path;inp.audio.setAttribute('aria-label',g.id+' '+t('input'));wave(inp.svg,input.waveform_peaks_64,g.waveScale);const buttonsBox=document.createElement('div');buttonsBox.className='radius-buttons';buttonsBox.setAttribute('role','group');buttonsBox.setAttribute('aria-label',t('radius'));const buttons=[];for(const f of g.files.filter(f=>f.role!=='input').sort((a,b)=>a.radius-b.radius)){const b=document.createElement('button');b.type='button';b.dataset.r=f.radius;b.textContent=f.radius+' m';buttonsBox.append(b);buttons.push(b)}out.heading.append(buttonsBox);const caption=document.createElement('p');caption.className='audio-caption';card.append(head,players,caption);wrap.append(card);const state={g,inp,out,buttons,title,meta,cat,caption,radius:g.defaultRadius||2};audioStates.set(g.id,state);buttons.forEach(b=>b.addEventListener('click',()=>updateOutput(state,Number(b.dataset.r))));updateOutput(state,state.radius);});translateAudio()}
function translateAudio(){for(const state of audioStates.values()){const {g,inp,out,title,meta,cat,caption}=state;title.textContent=lang==='zh'?g.titleZh:g.titleEn;cat.textContent=t(g.category+'Category');meta.textContent=`Profile ${g.profile} · ${t('sample')} ${g.sampleIndex} · ${g.distances.map(d=>d.toFixed(2)+' m').join(' / ')}`;caption.textContent=lang==='zh'?g.captionZh:g.captionEn;inp.title.textContent=t('input');out.title.textContent=t('output');inp.detail.textContent=`16 kHz · 4.5 s · ${t('waveNote')}`;inp.audio.setAttribute('aria-label',g.id+' '+t('input'));state.out.heading.querySelector('[role=group]').setAttribute('aria-label',t('radius'));updateOutput(state,state.radius,false)}}
function applyLanguage(){document.documentElement.lang=lang==='zh'?'zh-CN':'en';document.querySelectorAll('[data-i18n]').forEach(el=>{const k=el.dataset.i18n;el.innerHTML=t(k)});$('language').innerHTML=lang==='zh'?'EN <span aria-hidden="true">↗</span>':'中文 <span aria-hidden="true">↗</span>';$('language').setAttribute('aria-label',lang==='zh'?'Switch to English':'切换中文');setupSamples();drawChart();translateAudio();try{localStorage.setItem('radius-demo-lang',lang)}catch{}}
$('language').addEventListener('click',()=>{lang=lang==='zh'?'en':'zh';applyLanguage()});$('sampleSelect').addEventListener('change',e=>{activeSample=Number(e.target.value);drawChart()});document.querySelectorAll('[data-sample]').forEach(b=>b.addEventListener('click',()=>{activeSample=Number(b.dataset.sample);$('sampleSelect').value=String(activeSample);drawChart()}));$('axisSelect').addEventListener('change',drawChart);$('radiusSlider').addEventListener('input',e=>{pointIndex=Number(e.target.value);drawChart()});$('radiusChart').addEventListener('pointerdown',e=>{const rect=e.currentTarget.getBoundingClientRect();const vx=(e.clientX-rect.left)/rect.width*1000;pointIndex=Math.max(0,Math.min(40,Math.round((vx-72)/(1000-72-26)*40)));$('radiusSlider').value=pointIndex;drawChart()});
$('downloadSample').addEventListener('click',()=>{const s=curves.find(s=>s.id===activeSample);const rows=['sample_index,profile,source_distances_m,radius_m,output_rms_db,target_speakers,output_level_db_vs_input,delta_output_rms_db_vs_prev',...s.points.map(p=>[s.id,s.profile,s.distances.join(';'),p[0].toFixed(1),p[1],p[2],p[3],p[4]??''].join(','))];const blob=new Blob(['\uFEFF'+rows.join('\r\n')],{type:'text/csv;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`radius-sample-${s.id}.csv`;document.body.append(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)});
setupSamples();buildAudio();applyLanguage();
})();
